# Wiki Index

_Generated by the p:wiki reindex tool — refresh with `/p:wiki`. Do not edit by hand._

## overview
- [prompt-heaven Overview](overview.md) — What prompt-heaven is and a map to the documentation wiki.

## subsystem
- [Minion Agents](subsystems/agents.md) — Delegate-able sub-agents invoked via the Task tool to keep the main context clean.
- [Post-edit Hooks](subsystems/hooks.md) — Claude Code hooks that auto-run after edits to enforce quality and tool routing.
- [Scripts & MCP Servers](subsystems/scripts.md) — Standalone Python scripts -- MCP servers and requirements.yaml task utilities.
- [Skills](subsystems/skills.md) — Loadable knowledge packs activated on-demand or via Skill permissions.
- [Test Fleet](subsystems/tests.md) — The stdlib-only functional test fleet — one explicit entry point, a two-layer harness, case counts written down once and machine-checked, and a severity model where FAIL is reserved for rules that cannot flap.

## component
- [Documentation Wiki Engine](components/wiki-engine.md) — The p:wiki skill, mcp-wiki server, and librarian minion that maintain the docs/ wiki.

## reference
- [requirements.yaml — the machine-checkable half of the plan](reference/requirements-yaml.md) — Why the plan-to-implement handoff splits into a prose plan and a validated YAML task graph, and what each load-bearing choice in the YAML buys.

## concept
- [DuckDuckGo Bot Detection Research](concepts/spec-ddg.md) — Why DDG blocks Python HTTP clients, and the DDG-first/Bing-fallback strategy used by the search script.
- [The ClaudeCode layer contract](concepts/layer-contract.md) — Why skills, agents and fragments are three non-overlapping layers, what an executor minion is, and why sub-agent nesting is capped below what the harness can actually do.

## spec
- [Implementation Plan: Unify mcp-clangd + mcp-cuda into mcp-purity (Phase 0)](specs/spec-purity-unification.md) — Phase 0 implementation plan for folding clangd + cuda into mcp-purity behind purity_call. Decision recorded in adr 0001.
- [Phase 1: luals as the second LSP backend behind purity_call](specs/spec-purity-luals-phase1.md) — How lua-language-server was folded in behind the unchanged purity_call entry point via a BaseLspClient extraction and a per-language divergence layer, plus the security-hardening pass that shipped with it.
- [Sandboxed CLI Runner — sandbox-run skill + sbx helper + PreToolUse gate](feature-implementation-plan.md) — Approve-the-wrapper-not-the-command trust-boundary shift — a stdlib-only sbx sandbox helper (Seatbelt/bwrap) plus a grant-only PreToolUse gate that auto-allows only a clean, contained, single sbx invocation and prompts on everything else.

## adr
- [A path-spelled deny protects the spelling, not the file](adr/0007-a-path-spelled-deny-protects-the-spelling.md) — Decision to open the ~/.claude read-deny fail-closed (two named subdirectories rather than a list of secrets to hide), and to close the symlink name-divergence with realpath-derived write-only shadow denies emitted as the profile's last file rules.
- [A serialized read loop makes one slow call look like a dead server](adr/0008-a-serialized-read-loop-looks-like-a-dead-server.md) — Decision to dispatch each MCP request as its own task with a reader thread nothing can take, and to stop treating an error payload as "keep waiting" — the two halves that turned one transient 503 into a restart-only outage — then to convert all ten live servers, with the concurrency decision audited per server rather than copied.
- [Approve the wrapper, not the command](adr/0005-approve-the-wrapper-not-the-command.md) — Decision to shift the Bash trust boundary from an ever-growing per-command allowlist to one audited OS-kernel containment wrapper (sbx), auto-allowed by a grant-only, fail-to-prompt PreToolUse gate matched by canonical-path identity.
- [Never pin a browser impersonation version; the two backends are asymmetric on purpose](adr/0004-never-pin-a-browser-impersonation-version.md) — Decision to forbid pinned impersonation names on primp while tolerating them on curl_cffi, because primp rots silently into a random browser, and to make the retry ladder escalate by engine on a platform-dependent list.
- [The index claims no freshness; frontmatter carries only editorial intent](adr/0002-index-claims-no-freshness.md) — Decision to rename the hand-written status value current to active, suppress it in INDEX.md, and forbid the two values that collide with git-measured freshness states.
- [The second executor — who closes the loop on a reported bug](adr/0006-the-second-executor.md) — Decision to add a second executor minion (Quint) that carries a reported bug end-to-end, gated by a shape-based containment rule and a reproduce-or-verify evidence rule, plus the six designs rejected on the way.
- [The wiki trigger lives in the tool description, not in CLAUDE.md](adr/0003-the-trigger-travels-with-the-tool.md) — Decision to move the wiki-consultation trigger into the wiki_call tool description so it travels with the plugin, leaving CLAUDE.md only what is project-specific.
- [Unify mcp-clangd and mcp-cuda into mcp-purity](adr/0001-purity-server-unification.md) — Decision to fold the clangd and cuda LSP servers into mcp-purity behind purity_call via a layered backend architecture.
